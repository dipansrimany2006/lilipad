{
  "name": "lilipad-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev --remote",
    "dev:local": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute lilipad-db --remote --file=../schema.sql",
    "db:init:local": "wrangler d1 execute lilipad-db --local --file=../schema.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241112.0",
    "typescript": "^5.0.0",
    "wrangler": "^3.91.0"
  }
}
